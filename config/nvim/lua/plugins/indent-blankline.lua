local blankline = require("indent_blankline")

blankline.setup({
	-- buftype_exclude = { "terminal", "nofile" },
	-- filetype_exclude = { "packer", "windline" },
	show_current_context = true,
	show_first_indent_level = false,
	-- show_trailing_blankline_indent = false,
})
